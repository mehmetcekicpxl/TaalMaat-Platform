@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Gericht naar inlogpagina met terugkeer-URL
        var currentUri = Navigation.Uri;
        Navigation.NavigateTo($"/login?returnUrl={Uri.EscapeDataString(currentUri)}", replace: true);
    }
}
